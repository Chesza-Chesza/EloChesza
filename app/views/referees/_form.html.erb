<div class="container">  
  <%= form_with(model: referee, class: "form-group mb-4") do |form| %>
    <% if referee.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(referee.errors.count, "error") %> prohibited this referee from being saved:</h2>

        <ul>
          <% referee.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :fide_id %>
      <%= form.number_field :fide_id, class: "form-control"  %>
    </div>

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-control"  %>
    </div>

    <div class="field">
      <%= form.label :title %>
      <%= form.number_field :title, class: "form-control"  %>
    </div>

    <div class="actions">
      <%= form.submit :class => 'btn btn-info my-3'%>
    </div>
  <% end %>
</div>
