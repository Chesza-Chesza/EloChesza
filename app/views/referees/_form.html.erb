<%= form_with(model: referee) do |form| %>
  <% if referee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(referee.errors.count, "error") %> prohibited this referee from being saved:</h2>

      <ul>
        <% referee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fide_id %>
    <%= form.number_field :fide_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.number_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
